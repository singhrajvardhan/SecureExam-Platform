<!DOCTYPE html>
<html lang="en">
<head>
    
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>SmartExam | AI-Powered Proctoring | Next-Gen Exam Integrity</title>
    
    <!-- SEO Meta Tags -->
    <meta name="description" content="SmartExam: AI-powered online exam system with face tracking, tab-switch detection, auto proctoring, real-time analytics, and automatic scoring.">
    <meta name="author" content="Rajvardhan Singh Badgujar">
    
    <!-- Fonts & Icons -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
        <!-- SEO Meta Tags - Comprehensive -->
    <meta name="description" content="SmartExam: AI-powered online exam system with face tracking, tab-switch detection, auto proctoring, real-time analytics, and automatic scoring. Secure platform for teachers and students.">
    <meta name="keywords" content="online exam system, AI proctoring, face detection exam, tab switch detection, auto scoring, teacher dashboard, student assessment, suspicious behavior alerts, online test platform">
    <meta name="author" content="Rajvardhan Singh Badgujar">
    <meta name="robots" content="index, follow">
    <meta name="googlebot" content="index, follow">
    <meta name="language" content="English">
    <meta name="revisit-after" content="7 days">
    <meta name="distribution" content="global">
    <meta name="rating" content="General">
    
    <!-- Open Graph / Social Media Meta Tags -->
    <meta property="og:title" content="SmartExam - AI Proctored Online Exam System">
    <meta property="og:description" content="Next-gen exam platform with face tracking, real-time monitoring, and automatic scoring. Perfect for educational institutions.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://www.smartexam.com">
    <meta property="og:image" content="https://www.smartexam.com/og-exam-image.jpg">
    <meta property="og:site_name" content="SmartExam">
    
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="SmartExam - AI-Powered Online Exam System">
    <meta name="twitter:description" content="Secure online exams with intelligent proctoring, face detection, and detailed analytics.">
    <meta name="twitter:image" content="https://www.smartexam.com/twitter-card.jpg">
    
    <!-- Canonical URL -->
      <link rel="canonical" href="https://www.rajvardhansingh.in/" />
    <!-- Geo & Regional -->
    <meta name="geo.region" content="IN">
    <meta name="geo.placename" content="India">
    
    <!-- Mobile & App Capability -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#0f172a">
    
    <!-- Favicon & Icons -->
    <link rel="icon" type="image/png" sizes="32x32" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='%233b82f6'%3E%3Cpath d='M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5'/%3E%3C/svg%3E">
    <link rel="apple-touch-icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='%233b82f6'%3E%3Cpath d='M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5'/%3E%3C/svg%3E">
    
    <!-- Preload & Performance -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;14..32,400;14..32,500;14..32,600;14..32,700;14..32,800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <!-- JSON-LD Structured Data for SEO (Rich Results) -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebApplication",
      "name": "SmartExam Online Exam System",
      "description": "AI-powered online examination platform with proctoring, face detection, tab switch monitoring, automatic scoring, and teacher dashboards.",
      "url": "https://www.smartexam.com",
      "applicationCategory": "EducationApplication",
      "operatingSystem": "All",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      },
      "creator": {
        "@type": "Person",
        "name": "Rajvardhan Singh Badgujar",
        "url": "https://www.rajvardhansingh.in"
      },
      "featureList": "Face tracking and detection, Tab switching detection, Suspicious behavior alerts, Auto proctoring system, Real-time activity monitoring, Automatic scoring with negative marking, Teacher dashboard, Student analytics",
      "audience": {
        "@type": "EducationalAudience",
        "educationalRole": "Student, Teacher"
      }
    }
    </script>
    
    <!-- BreadcrumbList Structured Data -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "Home",
          "item": "https://www.smartexam.com"
        },
        {
          "@type": "ListItem",
          "position": 2,
          "name": "Student Portal",
          "item": "https://www.smartexam.com/student/login.php"
        },
        {
          "@type": "ListItem",
          "position": 3,
          "name": "Teacher Portal",
          "item": "https://www.smartexam.com/teacher/login.php"
        }
      ]
    }
    </script>
    

    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: #0a0c15;
            color: #ffffff;
            overflow-x: hidden;
            min-height: 100vh;
        }

        /* ========== CUSTOM AMAZING BACKGROUND ========== */
        .bg-universe {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: radial-gradient(ellipse at 20% 30%, #0f0c29, #0a0a1a, #02020c);
        }

        /* Animated glowing grid */
        .grid-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background-image: 
                linear-gradient(rgba(59, 130, 246, 0.08) 1px, transparent 1px),
                linear-gradient(90deg, rgba(59, 130, 246, 0.08) 1px, transparent 1px);
            background-size: 60px 60px;
            animation: gridFloat 20s linear infinite;
            pointer-events: none;
        }

        @keyframes gridFloat {
            0% { transform: translateY(0px) translateX(0px); opacity: 0.5; }
            100% { transform: translateY(60px) translateX(60px); opacity: 1; }
        }

        /* Orbiting light orbs */
        .orb {
            position: fixed;
            border-radius: 50%;
            filter: blur(80px);
            z-index: -1;
            pointer-events: none;
            animation: orbMove 25s infinite alternate ease-in-out;
        }

        .orb-1 {
            width: 50vw;
            height: 50vw;
            background: radial-gradient(circle, rgba(59, 130, 246, 0.4), transparent);
            top: -20%;
            left: -20%;
            animation-duration: 30s;
        }

        .orb-2 {
            width: 60vw;
            height: 60vw;
            background: radial-gradient(circle, rgba(139, 92, 246, 0.35), transparent);
            bottom: -30%;
            right: -20%;
            animation-duration: 35s;
            animation-direction: alternate-reverse;
        }

        .orb-3 {
            width: 40vw;
            height: 40vw;
            background: radial-gradient(circle, rgba(236, 72, 153, 0.25), transparent);
            top: 40%;
            left: 60%;
            animation-duration: 28s;
        }

        @keyframes orbMove {
            0% { transform: translate(0, 0) scale(1); opacity: 0.5; }
            100% { transform: translate(5%, 8%) scale(1.2); opacity: 0.9; }
        }

        /* Floating particles */
        .particle-field {
            position: fixed;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        .particle {
            position: absolute;
            background: linear-gradient(135deg, #38bdf8, #a855f7);
            border-radius: 50%;
            opacity: 0.5;
            animation: particleFloat 12s infinite alternate;
        }

        @keyframes particleFloat {
            0% { transform: translateY(0px) translateX(0px); opacity: 0.2; }
            100% { transform: translateY(-80px) translateX(50px); opacity: 0.8; }
        }

        /* Main Container */
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 1.5rem 2rem 2rem;
            position: relative;
            z-index: 10;
        }

        /* Glass Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            background: rgba(10, 15, 30, 0.6);
            backdrop-filter: blur(16px);
            border-radius: 2rem;
            padding: 0.8rem 2rem;
            margin-bottom: 2rem;
            border: 1px solid rgba(59, 130, 246, 0.4);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            background: linear-gradient(145deg, #3b82f6, #8b5cf6);
            width: 48px;
            height: 48px;
            border-radius: 1.2rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.6rem;
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
            animation: pulseGlow 2s infinite;
        }

        @keyframes pulseGlow {
            0%, 100% { box-shadow: 0 0 0 0 rgba(59, 130, 246, 0.4); }
            50% { box-shadow: 0 0 0 12px rgba(59, 130, 246, 0); }
        }

        .logo-text {
            font-size: 1.8rem;
            font-weight: 800;
            background: linear-gradient(135deg, #fff, #a5b4fc);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .nav-links {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .nav-btn {
            color: #cbd5e6;
            text-decoration: none;
            padding: 0.6rem 1.3rem;
            border-radius: 2rem;
            font-weight: 500;
            transition: all 0.3s;
            cursor: pointer;
            background: rgba(255, 255, 255, 0.03);
        }

        .nav-btn i { margin-right: 8px; }
        .nav-btn:hover, .nav-btn.active {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.4), rgba(139, 92, 246, 0.3));
            color: white;
            border-bottom: 2px solid #3b82f6;
        }

        /* Glass Card */
        .glass-card {
            background: rgba(15, 25, 45, 0.55);
            backdrop-filter: blur(12px);
            border-radius: 2rem;
            border: 1px solid rgba(59, 130, 246, 0.35);
            padding: 2rem;
            margin-bottom: 2rem;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .glass-card:hover {
            box-shadow: 0 15px 40px rgba(59, 130, 246, 0.2);
        }

        h1 {
            font-size: clamp(2rem, 5.5vw, 3.8rem);
            font-weight: 800;
            background: linear-gradient(135deg, #38bdf8, #c084fc, #f472b6);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 1rem;
            line-height: 1.2;
        }

        .section-title {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            border-left: 5px solid #3b82f6;
            padding-left: 1rem;
        }

        /* Portal Cards - Now with proper links */
        .portal-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 1.8rem;
            margin: 2rem 0;
        }

        .portal-card {
            flex: 1;
            min-width: 250px;
            background: linear-gradient(145deg, rgba(30, 41, 59, 0.8), rgba(15, 23, 42, 0.9));
            border-radius: 1.8rem;
            padding: 2rem 1.5rem;
            border: 2px solid rgba(59, 130, 246, 0.6);
            transition: all 0.35s cubic-bezier(0.2, 0.9, 0.4, 1.1);
            text-align: center;
            position: relative;
            overflow: hidden;
            text-decoration: none;
            display: block;
            cursor: pointer;
        }

        .portal-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(59, 130, 246, 0.2), transparent 70%);
            opacity: 0;
            transition: opacity 0.5s;
        }

        .portal-card:hover::before {
            opacity: 1;
        }

        .portal-card:hover {
            transform: translateY(-12px);
            border-color: #3b82f6;
            box-shadow: 0 25px 45px -12px rgba(59, 130, 246, 0.5);
        }

        .portal-icon { font-size: 3rem; margin-bottom: 1rem; }
        .portal-card h3 { font-size: 1.6rem; margin-bottom: 0.5rem; color: white; }
        .portal-card p { color: #cbd5e6; font-size: 0.9rem; }
        .portal-badge {
            position: absolute;
            top: 12px;
            right: 12px;
            background: #3b82f6;
            padding: 0.2rem 0.8rem;
            border-radius: 30px;
            font-size: 0.7rem;
            font-weight: bold;
            color: white;
        }

        /* Forms Row */
        .forms-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(330px, 1fr));
            gap: 2rem;
            margin-top: 1.5rem;
        }

        .form-card {
            background: rgba(0, 0, 0, 0.5);
            border-radius: 1.6rem;
            padding: 1.8rem;
            border-top: 4px solid #3b82f6;
            transition: all 0.3s;
        }

        .form-card:hover {
            background: rgba(0, 0, 0, 0.7);
            transform: translateY(-5px);
        }

        .form-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1.2rem;
            display: flex;
            align-items: center;
            gap: 10px;
            color: white;
        }

        .input-group {
            margin-bottom: 1rem;
        }

        .input-group label {
            display: block;
            margin-bottom: 0.4rem;
            font-weight: 500;
            font-size: 0.85rem;
            color: #cbd5e1;
        }

        .input-group input, .input-group select, textarea {
            width: 100%;
            padding: 0.8rem 1rem;
            background: rgba(255, 255, 255, 0.08);
            border: 1px solid rgba(59, 130, 246, 0.4);
            border-radius: 1rem;
            color: white;
            font-family: 'Inter', sans-serif;
            transition: 0.2s;
        }

        .input-group input:focus, .input-group select:focus, textarea:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: 0 0 10px rgba(59, 130, 246, 0.3);
        }

        .btn-primary {
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            border: none;
            padding: 0.9rem;
            border-radius: 2rem;
            font-weight: 700;
            width: 100%;
            cursor: pointer;
            color: white;
            transition: all 0.3s;
            font-size: 1rem;
        }

        .btn-primary:hover {
            transform: scale(1.02);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.5);
        }

        /* Proctoring Grid */
        .proctor-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.2rem;
            margin: 1.5rem 0;
        }

        .proctor-item {
            background: rgba(0, 0, 0, 0.4);
            border-radius: 1.2rem;
            padding: 1.2rem;
            border-left: 4px solid #3b82f6;
            transition: 0.2s;
        }

        .proctor-item i { font-size: 2rem; color: #3b82f6; margin-bottom: 0.6rem; display: block; }
        .proctor-item h4 { font-size: 1.2rem; margin-bottom: 0.4rem; color: white; }
        .proctor-item p { color: #cbd5e6; font-size: 0.85rem; }

        /* Stats */
        .stats-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
            margin: 1.5rem 0;
        }

        .stat-card {
            flex: 1;
            background: rgba(59, 130, 246, 0.15);
            border-radius: 1.2rem;
            padding: 1rem;
            text-align: center;
            backdrop-filter: blur(4px);
        }

        .stat-number {
            font-size: 2.2rem;
            font-weight: 800;
            color: #60a5fa;
        }

        /* Ticket List */
        .ticket-list {
            max-height: 300px;
            overflow-y: auto;
        }

        .ticket-item {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 1rem;
            padding: 0.8rem;
            margin-bottom: 0.8rem;
            border-left: 3px solid #8b5cf6;
            color: #e2e8f0;
        }

        .footer {
            text-align: center;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid rgba(59, 130, 246, 0.3);
            font-size: 0.85rem;
            color: #94a3b8;
        }

        .footer a {
            color: #60a5fa;
            text-decoration: none;
        }

        .hidden-section {
            display: none;
        }

        @media (max-width: 768px) {
            .container { padding: 1rem; }
            .navbar { flex-direction: column; gap: 1rem; }
        }
    </style>
</head>
<body>

<!-- Custom Amazing Background Elements -->
<div class="bg-universe"></div>
<div class="grid-overlay"></div>
<div class="orb orb-1"></div>
<div class="orb orb-2"></div>
<div class="orb orb-3"></div>
<div class="particle-field" id="particleField"></div>

<div class="container">
    <!-- Navigation -->
    <div class="navbar">
        <div class="logo">
            <div class="logo-icon"><i class="fas fa-brain"></i></div>
            <div class="logo-text">SmartExam<span style="font-size:0.9rem;">®</span></div>
        </div>
        <div class="nav-links">
            <div class="nav-btn active" data-section="home"><i class="fas fa-home"></i> Home</div>
         
          <div class="nav-btn" onclick="window.location.href='support.php'">
    <i class="fas fa-headset"></i> Support
</div>
    </div>

    <!-- HOME SECTION -->
    <div id="homeSection">
        <div class="glass-card">
            <h1>AI-Powered <span style="background: linear-gradient(120deg,#38bdf8,#c084fc); -webkit-background-clip:text; background-clip:text; color:transparent;">Exam Integrity</span> Platform</h1>
            <p style="font-size: 1.1rem; color: #cbd5e6; margin-bottom: 1rem;">Real-time face tracking · Tab-switch detection · Auto proctoring · Intelligent scoring</p>
            
            <!-- Portal Cards with proper hyperlinks -->
            <div class="portal-grid">
                <!-- Student Portal Link -->
                <a href="student/login.php" class="portal-card" data-target="studentFormCard">
                    <div class="portal-badge"><i class="fas fa-star"></i> Active</div>
                    <div class="portal-icon"><i class="fas fa-graduation-cap" style="color:#10b981;"></i></div>
                    <h3>Student Portal</h3>
                    <p>AI-proctored exams & instant results</p>
                    <span style="display: inline-block; margin-top: 12px; font-size: 0.75rem; color: #10b981;">→ Access Dashboard</span>
                </a>
                
                <!-- Teacher Portal Link -->
                <a href="teacher/login.php" class="portal-card" data-target="teacherFormCard">
                    <div class="portal-badge"><i class="fas fa-chart-line"></i> Live</div>
                    <div class="portal-icon"><i class="fas fa-chalkboard-user" style="color:#f97316;"></i></div>
                    <h3>Teacher Portal</h3>
                    <p>Monitor analytics & proctoring logs</p>
                    <span style="display: inline-block; margin-top: 12px; font-size: 0.75rem; color: #f97316;">→ Teacher Hub</span>
                </a>
                
                <!-- Registration Link (NEW - points to register page) -->
                <a href="student/register.php" class="portal-card" data-target="registerFormCard">
                    <div class="portal-badge"><i class="fas fa-fire"></i> New</div>
                    <div class="portal-icon"><i class="fas fa-user-plus" style="color:#a855f7;"></i></div>
                    <h3>Register Now</h3>
                    <p>Create free student account</p>
                    <span style="display: inline-block; margin-top: 12px; font-size: 0.75rem; color: #a855f7;">→ Get Started</span>
                </a>
            </div>

            <!-- Login & Registration Forms (for quick demo access) -->
         
    </div>

    <!-- PROCTORING SECTION -->
    <div id="proctoringSection" class="hidden-section">
        <div class="glass-card">
            <div class="section-title"><i class="fas fa-microchip"></i> Advanced Proctoring Intelligence</div>
            <div class="proctor-grid">
                <div class="proctor-item"><i class="fas fa-face-smile"></i><h4>Liveness & Face Tracking</h4><p>Continuous face detection, multiple face alerts, attention tracking with AI.</p></div>
                <div class="proctor-item"><i class="fas fa-window-maximize"></i><h4>Tab / Window Violation</h4><p>Real-time detection of tab switches, window changes with timestamp logging.</p></div>
                <div class="proctor-item"><i class="fas fa-robot"></i><h4>Auto Proctoring Engine</h4><p>AI flags suspicious behavior, gaze deviation, audio anomalies, background motion.</p></div>
                <div class="proctor-item"><i class="fas fa-chart-line"></i><h4>Live Monitoring Dashboard</h4><p>Teachers view live camera feeds, violation heatmaps & instant alerts.</p></div>
                <div class="proctor-item"><i class="fas fa-calculator"></i><h4>Auto Scoring + Negative Marking</h4><p>Instant evaluation with customizable negative marking rules.</p></div>
                <div class="proctor-item"><i class="fas fa-shield-alt"></i><h4>Behavioral Analytics</h4><p>Pattern recognition for cheating attempts, copy-paste detection.</p></div>
            </div>
            <div class="stats-grid">
                <div class="stat-card"><div class="stat-number">99.8%</div><div>Detection Accuracy</div></div>
                <div class="stat-card"><div class="stat-number">&lt;1.2s</div><div>Alert Latency</div></div>
                <div class="stat-card"><div class="stat-number">24/7</div><div>AI Shield Active</div></div>
            </div>
        </div>
    </div>

    <!-- ANALYTICS SECTION -->
    <div id="analyticsSection" class="hidden-section">
        <div class="glass-card">
            <div class="section-title"><i class="fas fa-chart-pie"></i> Real-time Analytics & Metrics</div>
            <div class="stats-grid">
                <div class="stat-card"><div class="stat-number" id="examCount">5,247</div><div>Exams Conducted</div></div>
                <div class="stat-card"><div class="stat-number" id="studentCount">18,456</div><div>Active Students</div></div>
                <div class="stat-card"><div class="stat-number" id="violationCount">589</div><div>Flagged Incidents</div></div>
                <div class="stat-card"><div class="stat-number" id="avgScore">88.2%</div><div>Average Score</div></div>
            </div>
            <div style="background: rgba(0,0,0,0.4); border-radius:1.2rem; padding:1.2rem; margin-top:1rem;">
                <h4 style="color:white; margin-bottom:0.8rem;"><i class="fas fa-chart-simple"></i> Platform Insights</h4>
                <p style="color:#cbd5e6;">📊 Integrity index increased by <strong>42%</strong> after AI proctoring integration. Cheating attempts reduced by 76%.</p>
                <p style="color:#cbd5e6; margin-top:0.5rem;">📈 Monthly active users growth: +58% YoY. Top subjects: Mathematics, Computer Science, Physics.</p>
                <div style="display:flex; gap:0.8rem; margin-top:1rem; flex-wrap:wrap;">
                    <span style="background:#3b82f6; padding:0.3rem 1rem; border-radius:20px;">🎯 Predictive Alerts</span>
                    <span style="background:#8b5cf6; padding:0.3rem 1rem; border-radius:20px;">📉 Violation Heatmap</span>
                </div>
            </div>
        </div>
    </div>

    <!-- SUPPORT SECTION -->
    <div id="supportSection" class="hidden-section">
        <div class="glass-card">
            <div class="section-title"><i class="fas fa-headset"></i> 24/7 Support & Ticketing</div>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px,1fr)); gap: 2rem;">
                <div>
                    <h4 style="color:white;"><i class="fas fa-pen-alt"></i> Submit a Ticket</h4>
                    <div class="input-group"><label>Full Name</label><input type="text" id="ticketName" placeholder="Your name"></div>
                    <div class="input-group"><label>Email Address</label><input type="email" id="ticketEmail" placeholder="contact@example.com"></div>
                    <div class="input-group"><label>Issue Category</label><select id="ticketCategory"><option>Technical Issue</option><option>Proctoring Alert</option><option>Account Access</option><option>Results Dispute</option></select></div>
                    <div class="input-group"><label>Message</label><textarea rows="3" id="ticketMsg" placeholder="Describe your issue..."></textarea></div>
                    <button class="btn-primary" id="submitTicketBtn"><i class="fas fa-paper-plane"></i> Send Request</button>
                    <div id="ticketSuccessMsg" style="margin-top: 1rem; color: #86efac;"></div>
                </div>
                <div>
                    <h4 style="color:white;">📬 Recent Support Tickets</h4>
                    <div id="ticketListContainer" class="ticket-list">
                        <div class="ticket-item"><i class="fas fa-clock"></i> <strong>Proctoring freeze</strong> – Resolved<br><small>Status: Closed</small></div>
                        <div class="ticket-item"><i class="fas fa-spinner"></i> <strong>Login error</strong> – In review<br><small>Status: Open</small></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="footer">
        <p><i class="far fa-copyright"></i> Copyright © 2026 <a href="#" style="color:#60a5fa; text-decoration:none;">Rajvardhan Singh Badgujar</a> | AI Proctoring · Next‑Gen Integrity Suite</p>
    </div>
</div>

<script>
    // Generate floating particles
    (function createParticles() {
        const container = document.getElementById('particleField');
        for (let i = 0; i < 80; i++) {
            const p = document.createElement('div');
            p.classList.add('particle');
            const size = Math.random() * 8 + 2;
            p.style.width = size + 'px';
            p.style.height = size + 'px';
            p.style.left = Math.random() * 100 + '%';
            p.style.top = Math.random() * 100 + '%';
            p.style.animationDuration = (Math.random() * 18 + 10) + 's';
            p.style.animationDelay = (Math.random() * 5) + 's';
            p.style.background = `radial-gradient(circle, rgba(59,130,246,${Math.random() * 0.5 + 0.2}), rgba(139,92,246,0.2))`;
            container.appendChild(p);
        }
    })();

    // Section navigation
    const sectionsMap = {
        home: document.getElementById('homeSection'),
        proctoring: document.getElementById('proctoringSection'),
        analytics: document.getElementById('analyticsSection'),
        support: document.getElementById('supportSection')
    };

    function showSection(sectionName) {
        Object.values(sectionsMap).forEach(section => {
            if (section) section.classList.add('hidden-section');
        });
        if (sectionsMap[sectionName]) sectionsMap[sectionName].classList.remove('hidden-section');
        document.querySelectorAll('.nav-btn').forEach(btn => btn.classList.remove('active'));
        document.querySelector(`.nav-btn[data-section="${sectionName}"]`).classList.add('active');
    }

    document.querySelectorAll('.nav-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            const section = btn.getAttribute('data-section');
            showSection(section);
        });
    });

    // Form handlers (demo simulations)
    document.getElementById('studentLoginBtn')?.addEventListener('click', () => {
        alert('✅ Student Login Successful!\nWelcome to AI-Proctored Dashboard.\n(Simulated environment)');
    });
    document.getElementById('teacherLoginBtn')?.addEventListener('click', () => {
        alert('📊 Teacher Access Granted!\nYou can monitor exams, live proctoring, and analytics.');
    });
    document.getElementById('registerBtn')?.addEventListener('click', () => {
        const name = document.getElementById('regName').value;
        const email = document.getElementById('regEmail').value;
        if (name && email) {
            document.getElementById('regMessage').innerHTML = `✅ Registration successful! Welcome ${name}. Verification email sent.`;
        } else {
            document.getElementById('regMessage').innerHTML = '⚠️ Please fill all fields.';
        }
    });

    // Ticket system
    let tickets = [
        { name: 'Alex', category: 'Proctoring Alert', msg: 'False positive flag detected', time: new Date() },
        { name: 'Maria', category: 'Technical Issue', msg: 'Camera not accessible during exam', time: new Date() }
    ];

    function renderTickets() {
        const container = document.getElementById('ticketListContainer');
        if (!container) return;
        if (tickets.length === 0) {
            container.innerHTML = '<div class="ticket-item">No tickets yet. Submit your request.</div>';
            return;
        }
        container.innerHTML = tickets.map(t => `
            <div class="ticket-item">
                <i class="fas fa-ticket-alt"></i> <strong>${t.category}</strong><br>
                ${t.msg.substring(0, 70)}...<br>
                <small>Status: Open | ${t.time.toLocaleTimeString()}</small>
            </div>
        `).join('');
    }

    document.getElementById('submitTicketBtn')?.addEventListener('click', () => {
        const name = document.getElementById('ticketName').value;
        const email = document.getElementById('ticketEmail').value;
        const category = document.getElementById('ticketCategory').value;
        const msg = document.getElementById('ticketMsg').value;
        if (!name || !email || !msg) {
            alert('Please fill all fields');
            return;
        }
        tickets.unshift({ name, category, msg, time: new Date() });
        renderTickets();
        document.getElementById('ticketSuccessMsg').innerHTML = '<i class="fas fa-check-circle"></i> Ticket submitted! Support will reply within 2 hours.';
        document.getElementById('ticketName').value = '';
        document.getElementById('ticketEmail').value = '';
        document.getElementById('ticketMsg').value = '';
        setTimeout(() => {
            document.getElementById('ticketSuccessMsg').innerHTML = '';
        }, 3000);
    });
    renderTickets();

    // Auto-updating analytics numbers
    setInterval(() => {
        const examElem = document.getElementById('examCount');
        const studentElem = document.getElementById('studentCount');
        if (examElem && examElem.closest('.hidden-section') === null) {
            examElem.innerText = Math.floor(5247 + Math.random() * 15);
            studentElem.innerText = Math.floor(18456 + Math.random() * 50);
        }
    }, 4000);

    // GSAP animations (if GSAP available, else fallback)
    if (typeof gsap !== 'undefined') {
        gsap.from(".glass-card", { duration: 0.8, y: 40, opacity: 0, ease: "power2.out" });
        gsap.from(".portal-card", { duration: 0.6, scale: 0.95, opacity: 0, stagger: 0.1, delay: 0.2 });
        gsap.from(".form-card", { duration: 0.6, y: 25, opacity: 0, stagger: 0.12, delay: 0.3 });
    }

    // Show home by default
    showSection('home');
</script>
</body>
</html>
